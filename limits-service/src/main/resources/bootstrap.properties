spring.application.name=limits-service
spring.profiles.active=dev


#limits-service.minimum=99
#limits-service.maximum=9999

# need to configure spring cloud config server uri
spring.cloud.config.uri=http://localhost:8888

# note after change in any properties value need to commit changes and also
# restart limit service and config server
